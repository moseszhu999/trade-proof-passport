# External Adoption Pilot acceptance lock

Exact base: `4a30a02fc5eefb94cc3793ea52dc2d4687f6c337`

This bounded implementation must remain:

- exactly two independently rooted full-artifact verified Pool adoptions;
- exactly one rejected duplicate Adoption Card;
- exactly two Proof Liquidity units;
- exactly one non-ranked, non-endorsement Directory entry;
- local file processing only;
- public, synthetic, or explicitly authorized data only;
- no new protocol schema, canonical ID, digest owner, database table, RPC, identity authority, attestation, ranking, payment, settlement, RWA, Token, wallet operation, or chain write.

A page view, empty fork, star, pull request, or merged pull request is never an adoption unit.
